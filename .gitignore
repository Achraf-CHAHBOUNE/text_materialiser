# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Archives
*.zip

# Secrets & runtime
.env
.anonymizer_state.json

# Input documents — NEVER commit (sensitive PII). Keep the empty folder only.
input/*
!input/.gitkeep

# Generated output — keep the empty folder only.
output/*
!output/.gitkeep
